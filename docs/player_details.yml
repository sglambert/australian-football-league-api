parameters:
  - name: team
    in: query
    type: string
    description: The team name
    default: Geelong
    required: false
  - name: current
    in: query
    type: boolean
    description: Details about the team's current or all-time playing list.
    default: True
    required: false
  - name: source
    in: query
    type: string
    enum: ['footywire', 'AFL', 'afltables']
    description: The data source. Note, only the 'afltables' source will return historical / non-current player details.
    default: AFL
    required: false
definitions:
  PlayerDetails:
    type: object
    properties:
      firstName:
        type: object
      surname:
        type: object
      id:
        type: object
      team:
        type: object
      season:
        type: object
      jumperNumber:
        type: object
      position:
        type: object
      providerId:
        type: object
      dateOfBirth:
        type: object
      draftYear:
        type: object
      heightInCm:
        type: object
      weightInKg:
        type: object
      recruitedFrom:
        type: object
      debutYear:
        type: object
      draftType:
        type: object
      draftPosition:
        type: object
      data_accessed:
        type: object
responses:
  200:
    description: JSON response with player details.
    schema:
      $ref: '#/definitions/PlayerDetails'
